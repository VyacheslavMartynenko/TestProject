package player.position;

import move.type.Move;
import observer.Subject;

public class Defender extends Player {

    public Defender(Move move, Subject subject) {
        super(move, subject);
    }
}
