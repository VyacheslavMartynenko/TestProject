package player.position;

import move.type.Move;
import observer.Subject;

public class Midfielder extends Player {
    public Midfielder(Move move, Subject subject) {
        super(move, subject);
    }
}
